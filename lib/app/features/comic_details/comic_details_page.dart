import 'package:flutter/material.dart';

class ComicDetails extends StatelessWidget {
  const ComicDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}